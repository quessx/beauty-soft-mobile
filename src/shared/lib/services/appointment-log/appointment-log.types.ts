export namespace AppointmentLogComponentTypes {
    export type scheduleState = 'day' | 'week';
    export const scheduleState = {
        day: 'day' as scheduleState,
        week: 'week' as scheduleState,
    };
}