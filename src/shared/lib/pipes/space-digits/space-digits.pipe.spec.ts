import { SpaceDigitsPipe } from './space-digits.pipe';

describe('SpaceDigitsPipe', () => {
  it('create an instance', () => {
    const pipe = new SpaceDigitsPipe();
    expect(pipe).toBeTruthy();
  });
});
